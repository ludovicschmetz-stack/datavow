# datavow

Data contract enforcement for modern data teams.

## Install

```bash
pip install datavow
```

## Quick start

```bash
datavow init my-project
```

This creates a `datavow.yaml` config and a `contracts/` directory with an example contract.
